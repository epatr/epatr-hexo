<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>
        <% if (page.title) { %><%= page.title %> — <% } %>
        <%= config.title %>
        <% if (!page.title) { %>
        | <%= config.subtitle %> | <%= config.description %>
        <% } %>
    </title>
</head>
<body>

<%- body %>

</body>
</html>